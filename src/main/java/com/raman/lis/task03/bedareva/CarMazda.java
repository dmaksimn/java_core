package com.raman.lis.task03.bedareva;

public class CarMazda extends Car {

    CarMazda(Color color, int year, WheelSize wheelSize, EngineVolume engineVolume) {
        super(color, Model.MAZDA, year, wheelSize, engineVolume);
    }
}
